import styled from 'styled-components';

const ThemeModeStyle = styled.div`

button { 
    border: none;
    background: none;

    svg path{
        transition: 1s;
    }
}

`

export default ThemeModeStyle;